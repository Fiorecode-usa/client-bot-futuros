# Los entornos por defecto están en:
#   .env.development  → npm run dev      (localhost:4001)
#   .env.production   → npm run build    (Railway)
#
# Opcional: crea .env local (gitignored) para sobreescribir cualquier valor.
